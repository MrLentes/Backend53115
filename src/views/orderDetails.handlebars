<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de la compra</title>
</head>
<body>
    <h1>Detalles de la compra</h1>
    <div class="order-summary">
        <h3>Numero del pedido: {{orderNumber}}</h3>
        <p>Fecha del pedido: {{orderDate}}</p>
        <p>Estado: {{status}}</p>
        <h3>Productos:</h3>
        {{#each items}}
        <div>
            <p>{{this.name}} - ${{this.price}} x {{this.quantity}}</p>
        </div>
        {{/each}}
        <h3>Total: ${{total}}</h3>
    </div>
    <div class="shipping-details">
        <h3>Informacion de la entrega</h3>
        <p>{{shippingAddress}}</p>
    </div>
</body>
</html>
